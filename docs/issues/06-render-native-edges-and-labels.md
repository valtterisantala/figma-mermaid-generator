# Issue 06 — Render editable native edges and edge labels

## Purpose

Complete the diagram by generating editable connections in normal Figma files.

## Scope

- render straight vector-based edges
- render arrowheads
- render edge labels as text nodes
- group edge parts clearly
- keep implementation compatible with Figma design files

## Notes

This should not depend on FigJam-only connector features.

v1 can keep edge routing simple.

## Definition of done

- edges are individually selectable/editable
- arrowheads render correctly
- edge labels are editable text
- output remains understandable in the Layers panel

## Dependencies

- 05 Render native Figma nodes for nodes, labels, and subgraphs